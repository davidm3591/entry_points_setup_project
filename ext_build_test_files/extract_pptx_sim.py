# Build sim for extraction_pptx.py

def read_file():
    """Build simulated extraction_pptx.py
       for entry script build
    """
    print(f"I have just run read_file from"
          f" function call in extract_tool.py"
        )