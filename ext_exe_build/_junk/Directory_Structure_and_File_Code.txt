Directory Structure and File Code

Master branch (working)
entry_points_setup_project
    > .git
    > ext_build
        > extract_build
            __pycache__
            > __init__.py
            > __main__.py
            > extract_startup.py
        > .gitignore
        > chk_call_from_main.py
                def test_extract_open():
                print("I fired from a call to test_extract_open in __main__.py")
        > Directory_Structure_and_File_Code.txt
        > setup.py
    > my_project
        > __init__.py
        > __main__.py
    > .gitignore
    > setup.py


extraction_build_setup branch
entry_points_setup_project
    > .git
    > ext_build
        > extract_build
            __pycache__
            
            > __init__.py (empty, but have to have it make setup work)
            
            > __main__.py
                    import sys
                    from extract_startup import extract_open (this is the module/function to load gui)
                    function: extract_open() (this is the function call to load gui in extraction_tool.py)
            
            > extract_startup.py (this file simulates extraction_tool.py with function that loads gui)
                    def extract_open(): (this simulates a function wrappipng the tkinter code)
                        print("I fired from a call to extract_open() in __main__.py")
        > .gitignore
        > chk_call_from_main.py
                def test_extract_open():
                print("I fired from a call to test_extract_open in __main__.py")
        > setup.py
                from setuptools import setup

                setup(name='extract_build',
                      version='0.1.0',
                      packages=['extract_build'], 
                      entry_points={
                          'console_scripts': [
                              'extract_build = extract_build.__main__:main'
                          ]
                      },
                      )
    > my_project
        > __init__.py
        > __main__.py
    > .gitignore
    > setup.py













ext_build
    > extract_build
        > __init__.py
        > __main__.py
    > .gitignore
    > chk_call_from_main.py
    > setup.py